The wcloud.jar must be excecuted from inside the bin folder of the src directory. 

In the command prompt (cmd on windows), navigate to the bin folder and from there enter the command:

java -cp ./wcloud.jar ie.gmit.dip.Runner